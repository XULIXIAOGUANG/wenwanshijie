# 文玩记录本 · PWA 部署指南

> 把 `webapp/` 这个文件夹里的内容（不是整个项目）发布到互联网，得到一个 HTTPS 网址，
> 用 iPhone 的 Safari 打开后「添加到主屏幕」，就能像真正的 App 一样全屏使用，还能离线打开。

**核心要求**：网页必须在 **HTTPS** 下访问，Safari 才允许安装为 PWA（本地 `localhost` 除外）。
下面三种方案任选其一，推荐方案一（免费）。

---

## 方案一：GitHub Pages（免费，推荐）

费用 ¥0，永久有效。需要先注册 GitHub 账号（免费，5 分钟）。

### 1. 注册账号
1. 浏览器打开 `https://github.com` → 点 **Sign up**
2. 填邮箱、密码、用户名（用户名自己起，比如 `wenwan2026`），完成邮箱验证

### 2. 新建仓库
1. 登录后点右上角 **+** → **New repository**
2. 仓库名填 `wenwan-notebook`（任意英文名都行）
3. 选 **Public**（免费）
4. 点 **Create repository**

### 3. 上传文件（全程网页操作，不用命令行）
1. 进入仓库 → 点 **Add file** → **Upload files**
2. 把 `webapp/` 文件夹里**所有内容**拖进去：
   - `index.html`
   - `manifest.json`
   - `sw.js`
   - `icons/` 文件夹（3 个图标）
   - `assets/` 文件夹（头像图）
3. 点 **Commit changes** 提交

### 4. 开启 Pages 并发布
1. 仓库页面 → **Settings** → 左侧 **Pages**
2. **Branch** 选 `main`，文件夹选 `/ (root)`，点 **Save**
3. 等 1~2 分钟，页面顶部会出现网址：`https://<你的用户名>.github.io/wenwan-notebook/`

### 5. iPhone 打开并使用
1. 用 iPhone Safari 打开上面的网址
2. 看到页面加载出首页后 → 点分享按钮 → **添加到主屏幕**
3. 桌面出现「文玩记录本」图标，点击即可全屏使用

> 以后更新：改完 `webapp/` 里的文件后，再走一遍「Upload files」覆盖同名文件即可，1 分钟搞定。

---

## 方案二：Vercel（免费备选）

GitHub 访问不畅时的备选。免费版足够个人使用。

1. 打开 `https://vercel.com` 注册（可用 GitHub 账号直接登录）
2. 点 **Add New...** → **Project**
3. 如果已传到 GitHub：选仓库直接导入；否则选 **Deploy without Git**，用拖拽方式把 `webapp/` 里的内容拖上去
4. 等部署完成，得到 `https://xxx.vercel.app` 网址
5. iPhone 打开 → 添加到主屏幕

---

## 方案三：微信云开发静态托管（国内访问最快，需付费）

仅当你觉得前两种打开太慢、且愿意花 ¥19.9/月时选这个。好处是国内访问快、和微信生态无缝。

1. 用微信开发者工具打开本项目
2. 点工具栏 **云开发** → 开通环境（选择「基础套餐」¥19.9/月，免费体验版不支持静态托管）
3. 进入云开发控制台 → **静态网站托管** → **上传文件** → 把 `webapp/` 里所有内容传上去
4. 开通后得到默认 HTTPS 域名（免备案），用 iPhone Safari 打开该域名
5. 添加到主屏幕即可

---

## iPhone 验证清单（安装后逐项检查）

| 检查项 | 操作 | 预期结果 |
|---|---|---|
| 全屏运行 | 点击主屏幕图标打开 | 无浏览器地址栏、无底部工具栏，像原生 App |
| 刘海适配 | 看首页顶部 | 标题栏避开刘海，不被遮挡 |
| 底部避让 | 看底部导航 | 5 个 tab 不被 Home Indicator（小白条）遮挡 |
| 离线可用 | 打开后开飞行模式，再点图标 | 能正常打开、能看已录入的数据 |
| 数据保存 | 录一条新藏品，杀掉 App 重开 | 数据还在（存在手机本地） |
| 状态栏颜色 | 看顶部状态栏 | 墨绿色 #5C8F6E 与页面一体 |

### 数据备份（重要）
- 设置页 → 导出备份，每月导一次存到微信「文件传输助手」
- 换手机、清 Safari 数据都会清掉本地记录，备份是唯一保险

---

## 常见问题

- **添加到主屏幕后没图标/白图标**：确认 `icons/` 已上传，刷新后再加一次
- **打开是网页不是全屏**：确认用的是 Safari（微信内置浏览器不支持），且通过「添加到主屏幕」打开
- **更新后还是旧版**：Service Worker 缓存了旧文件，双击图标打开一次，等几秒再关闭重开
- **iOS 提示「无法连接到 App」**：检查网址是否为 HTTPS，`http://` 不能安装
